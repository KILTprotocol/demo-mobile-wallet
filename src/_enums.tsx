export enum AsyncStatus {
  NotStarted,
  Pending,
  Success,
  Error,
}

export enum CredentialStatus {
  Valid,
  Revoked,
  AttestationPending,
}

export enum LoadingIndicatorSize {
  S,
  M,
  L,
}
