package com.ethernom.android.etherapi

interface SelectListener {
    fun onSelectSuccess(resultCode: Int)
    fun onSelectFail(resultCode: Int, hwSpecificError: Int)
    fun onDisconnect(resultCode: Int, hwSpecificError: Int)
}